name: reflected
kind: reflected_v2
routing_mode: dense
temperature: 1.0
pressure_scale: 1.0
pressure_lr: 0.05
pressure_decay: 0.0
pressure_warmup_steps: 1000
learnable_bias: true
shared_experts: 1
z_loss_weight: 0.0
aux_loss_weight: 0.0
# Structurally ignored in dense mode, kept for backwards compatibility
top_k: 1
capacity_factor: 0.5
drop_tokens: true
