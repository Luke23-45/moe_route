name: reflected_sparse
kind: reflected_v2
routing_mode: sparse
temperature: 1.0
pressure_scale: 1.0
pressure_lr: 0.05
pressure_decay: 0.0
pressure_warmup_steps: 1000
learnable_bias: true
shared_experts: 0
z_loss_weight: 0.0
aux_loss_weight: 0.0
# Mode B parameters
top_k: 2
# Small 32-expert experiments start highly imbalanced; keep capacity high enough
# that reflected-sparse is compared on routing behavior rather than overflow drops.
capacity_factor: 0.5
drop_tokens: true
