name: tiny_dense
vocab_size: ${tokenizer.vocab_size}
max_seq_len: ${data.sequence_length}
d_model: 128
n_layers: 2
n_heads: 4
d_ff: 256
dropout: 0.0
moe:
  enabled: false
  every_n_layers: 0
  num_experts: 0
  expert_hidden_size: 256
